{
  "formal_profiles": [
    {
      "description": "Development-focused formal profile: wave-1 ucagnn with learned fused scoring, explicit 1/2 branch depth, and CAGRA/kNN coverage across all thesis datasets.",
      "matrix": {
        "tier": "all",
        "presets": ["ucagnn"],
        "graph_methods": ["cagra", "knn"],
        "scoring_weight_modes": ["learned"]
      },
      "config_overrides": {
        "epochs": 60,
        "use_early_stopping": false,
        "lr": 0.001,
        "batch_size": 4096,
        "n_gnn_layers": 2,
        "interest_gnn_layers": 1,
        "conformity_gnn_layers": 2,
        "dropout": 0.1,
        "num_neighbors": [10, 5],
        "hard_negative_ratio": 0.0,
        "curriculum_phase1_end": 15,
        "curriculum_phase2_end": 30,
        "loss_schedule": "baseline"
      }
    },
    {
      "description": "Final matched-comparison profile: one end-stage shared-pipeline pass for ucagnn, LightGCN, and DICE-like on the default CAGRA graph with fixed fused-score mixing.",
      "matrix": {
        "tier": "all",
        "presets": ["ucagnn", "lightgcn", "dice_like"],
        "graph_methods": ["cagra"],
        "scoring_weight_modes": ["fixed"]
      },
      "config_overrides": {
        "epochs": 60,
        "use_early_stopping": false,
        "lr": 0.001,
        "batch_size": 4096,
        "n_gnn_layers": 2,
        "dropout": 0.1,
        "num_neighbors": [10, 5],
        "hard_negative_ratio": 0.0,
        "curriculum_phase1_end": 15,
        "curriculum_phase2_end": 30,
        "loss_schedule": "baseline"
      }
    }
  ],
  "recipes": {
    "ucagnn": {
      "description": "UCaGNN with mini-batch training on CAGRA edges.",
      "preset": "ucagnn",
      "overrides": {
        "graph_method": "cagra"
      }
    },
    "ucagnn_knn": {
      "description": "UCaGNN with mini-batch training on kNN edges.",
      "preset": "ucagnn",
      "overrides": {
        "graph_method": "knn"
      }
    },
    "lightgcn": {
      "description": "LightGCN baseline with mini-batch training on CAGRA edges.",
      "preset": "lightgcn",
      "overrides": {
        "graph_method": "cagra"
      }
    },
    "lightgcn_knn": {
      "description": "LightGCN baseline with mini-batch training on kNN edges.",
      "preset": "lightgcn",
      "overrides": {
        "graph_method": "knn"
      }
    },
    "dice_like": {
      "description": "DICE-like preset with mini-batch training on CAGRA edges.",
      "preset": "dice_like",
      "overrides": {
        "graph_method": "cagra"
      }
    },
    "dice_like_knn": {
      "description": "DICE-like preset with mini-batch training on kNN edges.",
      "preset": "dice_like",
      "overrides": {
        "graph_method": "knn"
      }
    }
  }
}